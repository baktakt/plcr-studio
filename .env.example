# Google Gemini API Configuration
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here

# Authentication (optional but HIGHLY RECOMMENDED for production)
# ⚠️ SECURITY WARNING:
# When AUTH_ENABLED=false, ANYONE can access your application and make API calls
# This exposes your GEMINI_API_KEY to public use and can incur significant costs
#
# ONLY set to "false" for LOCAL DEVELOPMENT on your personal machine (localhost)
# ALWAYS set to "true" for production, staging, or any internet-accessible deployment
AUTH_ENABLED=false

# NextAuth Configuration (required if AUTH_ENABLED=true)
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (required if AUTH_ENABLED=true)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Allowed Email Addresses (REQUIRED if AUTH_ENABLED=true)
# Comma-separated list of email addresses that are allowed to sign in
# ⚠️ IMPORTANT: If left empty, NO users will be able to sign in (security by default)
# You MUST explicitly list all users who should have access
ALLOWED_EMAILS=user1@example.com,user2@example.com,admin@yourdomain.com
